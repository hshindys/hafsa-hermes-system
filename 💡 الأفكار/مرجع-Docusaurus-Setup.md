---
type: report
title: 📚 Docusaurus — توثيق نظام هفصة (إعداد 2026-08-20)
tags: [docusaurus, docs, vault, documentation, setup]
---
# 📚 Docusaurus — توثيق نظام هفصة

## ✅ اللي اتعمل:
- ✅ مجلد `D:/vaults/Hafsa/docs-site` جاهز
- ✅ `package.json` (Docusaurus 3.6)
- ✅ `docusaurus.config.js` (بيتي من الـ vault: `index.md` + `💡 الأفكار/*.md` + `@حفصة/*.md`)
- ✅ `sidebars.js` (autogenerated)
- ✅ `src/css/custom.css` (ثيم وردي هفصة 💕)
- ⏳ `npm install` شغّال في background (pid 13072)

## 📌 طريقة التشغيل (لما يتخلص install):
```bash
cd D:/vaults/Hafsa/docs-site
npm run start    # dev server على http://localhost:3001
# أو
npm run build && npm run serve   # production
```

## 💡 المميزات:
- بيقرأ ماركدون الـ vault مباشرة (مفيش duplicate)
- sidebar أوتوماتيك من الـ folders
- ثيم عربي وردي (هفصة 💕)
- لما تضيفي ملف في الـ vault → يظهر في الموقع أوتوماتيك

## ⚠️ ملاحظة:
- Docusaurus محتاج build (مش instant زي Obsidian)
- لو حبيت تنشريه: `npm run build` → مجلد `build/` ترفعه على أي static host
