---
type: report
title: مراقبة النظام + تحسينات (Snapshot 2026-08-15)
tags: [مراقبة, تقرير, حالة-النظام, إنجازات, زيادات, تحسين]
---
# 📊 مراقبة النظام + تحسينات — Snapshot 2026-08-15

> تقرير شامل لحالة إعداد Hermes + Shared Memory Hub: إيه اللي اتعمل، الزيادات، والمعلّق.

## 🟢 المكتمل اليوم (الإنجازات)

### 1. Hermes Update → v0.20.1 ✅
- كان: v0.20.0 → دلوقتي **v0.20.1 (2026.8.13)**
- كل الإعدادات محفوظة: NVIDIA/Gemini/Claude providers + aliases + vault remote

### 2. Arabic FTS إصلاح (trigram) ✅
- rebuild لـ `conversation_fts` بـ **trigram tokenizer** (كان unicode61 بيقطّع العربي)
- الـ recall العربي شغّال 100%: قواعد/بحري/هفصة/طبي كلها بترجع hits
- اكتشاف مهم: ضفت `💡 الأفكار` لـ `_VAULT_INCLUDE_DIRS` (كانت مش متعمل ingest خالص!)

### 3. OpenCode + NVIDIA NIM ✅
- config: `opencode.json` (nvidia + tencent providers)
- launcher: `opencode-nvidia.bat` (مفتاح من .env مش plaintext)
- موديلات: glm/kimi/mini/dsv4 + tencent/hy3

### 4. Multi-Agent Worktree Setup ✅
- `multi_agent_worktree.py` — يعزل كل agent في git worktree + يفتح OpenCode + يبلّغ Telegram
- بديل Xirp المفتوح (Windows-compatible)

### 5. VeraCrypt ✅
- متنزّل ومثبّت (v1.26.29)
- سكريبت `setup_veracrypt_secrets.ps1` جاهز لحاوية الأسرار

### 6. Planika / Kanban ✅
- `planika.yml` (docker-compose) جاهز
- لوحة كانبان محلية: `🎯 المشاريع\لوحة-المهام-كانبان.md`

### 7. DeepSeek V4 Flash (`dsv4`) ✅
- متأكد شغّال على NVIDIA NIM مجاناً (test_dsv4.py)
- موجود في OpenCode config

### 8. Bot Mode Plugin + فريق Bots ✅
- plugin في `~/.hermes/desktop-plugins/hermes-bots/`
- profiles: `researcher` + `reportwriter`
- cron: **DailyResearch** (09:00، research_fetch، Telegram)

### 9. Shared Memory Hub
- **1230 ملف/row** في الـ db (كان 85 قبل شوية!)
- FTS: 1230 rows (trigram)
- scripts: 14 سكريبت جاهز (research, scrape, selfhosted, worktree, veracrypt...)

## 📈 الزيادات (إحصائيات)
- عدد الـ cron jobs: **17** (أدوية/متابعة/دلع/بحث/مراقبة)
- عدد الـ bots/profiles: **4** (default, hermes2, researcher, reportwriter)
- عدد الـ scripts في shared_memory: **14**
- حجم الـ db: 1230 rows (نما من 85 → 97 → 1230)

## 🟡 المعلّق / محتاج متابعة
1. ⚠️ **إلغاء مفاتيح NVIDIA الـ 3 القديمة** من build.nvidia.com (أمان حرج)
2. ⏳ **VPS** (Tencent free trial) لـ Buzz + Immich + RustDesk + DeepSeek Harness web UI
3. 🔐 **تفعيل VeraCrypt container** (لسه السكريبت ما اتشغّلش — محتاج تختار passphrase)
4. 🖥️ **Desktop app reload** عشان يظهر Bot Mode tab (Ctrl+K → Reload desktop plugins)
5. 🔴 **NVIDIA free credits**: GLM بيرجّع "balance too low" (dsv4 و kimi شغّالين)
6. 📋 **Planika docker**: محتاج Docker Desktop (إنته معندكش)

## 🔧 تحسينات مقترحة
- [ ] ربط `researcher` → `reportwriter` كـ bot-to-bot handoff أوتوماتيك (عبر cron)
- [ ] إضافة `gem`/`gop`/`cla`/`clo` للـ bots (لو حبيت Claude/Gemini في الـ bots)
- [ ] عمل backup دوري للـ db + vault (cron job تلقائي)
- [ ] إضافة monitoring script يراقب صحة الـ agents ويبلغ على Telegram

## 📌 حالة المفاتيح
- ✅ NVIDIA_API_KEY (الحالي) شغّال
- ⚠️ مفاتيح NVIDIA القديمة [REDACTED] ×3 → لازم إلغاء
- 🔑 Gemini/Claude: placeholders في .env (محتاج مفاتيح حقيقية لو عايز تستخدمهم)
